# Session cookie signing secret (min 32 characters)
SESSION_SECRET=your_32_character_random_session_secret_here

# At-rest data / API key encryption secret (min 32 characters)
KOKA_ENCRYPTION_KEY=your_32_character_random_encryption_key_here

# Allow new user registrations ("true" or "false")
ALLOW_SIGNUPS=true
